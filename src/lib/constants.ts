// Valid platforms
export enum Platforms {
  Gorilla = 'gorilla',
  jsPsych = 'jsPsych',
  Invalid = '',
}

// Valid controllers
export enum Controllers {
  Keyboard = 'keyboard',
  Spectrometer = 'spectrometer',
  Touch = 'touch',
}
