// Environments
export enum Environments {
  Gorilla = "gorilla",
  jsPsych = "jsPsych",
  Invalid = "",
}

// Controllers
export enum Controllers {
  Keyboard = "keyboard",
  Spectrometer = "spectrometer",
  Touch = "touch",
}
